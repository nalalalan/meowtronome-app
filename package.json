{
  "name": "meowtronome",
  "version": "0.1.0",
  "private": true,
  "description": "A quiet browser metronome using a real cat meow recording.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
