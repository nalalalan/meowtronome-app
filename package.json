{
  "name": "meowtronome",
  "version": "0.1.0",
  "private": true,
  "description": "A browser metronome whose beat is a synthesized meow.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
